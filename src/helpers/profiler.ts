export function onRenderProfiler(
  id: string,
  phase: string,
  actualDuration: number,
  baseDuration: number,
  startTime: number,
  commitTime: number,
) {
  //   console.log('---');
  //   console.log(Object.entries(arguments).map(([key, value]) => value));
  //   console.log('---');
}
